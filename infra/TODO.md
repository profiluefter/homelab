# TODO list

* [ ] Add new node in OCI cloud
* [ ] Add new node at third location
* [ ] Include DNS setup in Terraform configuration
  * maybe alternatively make kubernetes handle DNS so that it can automatically change it when the location that had the IP is down
